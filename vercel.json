{
  "installCommand": "cd frontend && corepack pnpm install --frozen-lockfile",
  "buildCommand": "npm run validate:vercel-env && cd frontend && corepack pnpm build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
