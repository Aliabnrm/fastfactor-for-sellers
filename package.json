{
  "name": "fastfactor-for-sellers",
  "private": true,
  "scripts": {
    "validate:vercel-env": "node scripts/validate-vercel-env.mjs",
    "build": "cd frontend && corepack pnpm build",
    "build:frontend": "cd frontend && corepack pnpm build",
    "build:backend": "cd backend && corepack pnpm build",
    "lint": "cd frontend && corepack pnpm lint"
  },
  "engines": {
    "node": ">=22"
  }
}
